# Once the server (wiselionlikeking/server) is deployed to a public URL,
# set this to switch the website from placeholder data to live data
# (merch products, songs, streams) — read-only, no auth needed.
#
# Leave BLANK / unset to keep using the placeholder content in src/data.ts.
# Example once deployed: VITE_API_URL=https://api.wiselion.app/api
VITE_API_URL=
